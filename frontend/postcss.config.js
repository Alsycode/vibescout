// FILE: frontend/postcss.config.js
// PURPOSE: PostCSS config — @tailwindcss/postcss only, no tailwind.config.js

const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};

export default config;
